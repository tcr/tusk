input:
  some_data: OK

build:
  - echo "$ENV_OK"
  - '[[ "$ENV_OK" = "OK" ]]; exit $?'
  - echo "$ENV_NOT_OK"
  - '[[ "$ENV_NOT_OK" != "OK" ]]; exit $?'
  - cat /tusk/input/some_data
  - '[[ "$(cat /tusk/input/some_data)" = "OK" ]]; exit $?'
